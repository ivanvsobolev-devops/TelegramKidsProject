# Telegram Kids Parent App

Flutter scaffold for the parent application.

## Scope

This package contains only the repository bootstrap shell. It does not implement parent authentication, approval queues, notifications, or approval history yet.

## Commands

```bash
flutter create --platforms=android --no-pub .
flutter pub get
flutter analyze
flutter test
flutter build apk --debug
```
